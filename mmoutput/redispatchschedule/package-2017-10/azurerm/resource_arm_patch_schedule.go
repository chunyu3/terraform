// ----------------------------------------------------------------------------
//
//     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
//
// ----------------------------------------------------------------------------
//
//     This file is automatically generated by Magic Modules and manual
//     changes will be clobbered when the file is regenerated.
//
//     Please read more about how to change this file at
//     https://github.com/Azure/magic-module-specs
//
// ----------------------------------------------------------------------------

package azurerm



func resourceArmPatchSchedule() *schema.Resource {
    return &schema.Resource{
        Create: resourceArmPatchScheduleCreateUpdate,
        Read: resourceArmPatchScheduleRead,
        Update: resourceArmPatchScheduleCreateUpdate,
        Delete: resourceArmPatchScheduleDelete,

        Importer: &schema.ResourceImporter{
            State: schema.ImportStatePassthrough,
        },


        Schema: map[string]*schema.Schema{
            "name": {
                Type: schema.TypeString,
                Required: true,
                ForceNew: true,
                ValidateFunc: validate.NoEmptyStrings,
            },

            "resource_group": azure.SchemaResourceGroupNameDiffSuppress(),

            "default": {
                Type: schema.TypeString,
                Required: true,
                ForceNew: true,
                ValidateFunc: validate.NoEmptyStrings,
            },

            "schedule_entries": {
                Type: schema.TypeList,
                Required: true,
                Elem: &schema.Resource{
                    Schema: map[string]*schema.Schema{
                        "day_of_week": {
                            Type: schema.TypeString,
                            Required: true,
                            ValidateFunc: validation.StringInSlice([]string{
                                string(redis.Monday),
                                string(redis.Tuesday),
                                string(redis.Wednesday),
                                string(redis.Thursday),
                                string(redis.Friday),
                                string(redis.Saturday),
                                string(redis.Sunday),
                                string(redis.Everyday),
                                string(redis.Weekend),
                            }, false),
                        },
                        "start_hour_utc": {
                            Type: schema.TypeInt,
                            Required: true,
                        },
                        "maintenance_window": {
                            Type: schema.TypeString,
                            Optional: true,
                            ValidateFunc: validateIso8601Duration(),
                        },
                    },
                },
            },

            "type": {
                Type: schema.TypeString,
                Computed: true,
            },
        },
    }
}

func resourceArmPatchScheduleCreateUpdate(d *schema.ResourceData, meta interface{}) error {
    client := meta.(*ArmClient).patchSchedulesClient
    ctx := meta.(*ArmClient).StopContext

    name := d.Get("name").(string)
    resourceGroup := d.Get("resource_group").(string)
    default := d.Get("default").(string)

    if features.ShouldResourcesBeImported() && d.IsNewResource() {
        existing, err := client.Get(ctx, resourceGroup, name, default)
        if err != nil {
            if !utils.ResponseWasNotFound(existing.Response) {
                return fmt.Errorf("Error checking for present of existing Patch Schedule %q (Default %q / Resource Group %q): %+v", name, default, resourceGroup, err)
            }
        }
        if existing.ID != nil && *existing.ID != "" {
            return tf.ImportAsExistsError("azurerm_patch_schedule", *existing.ID)
        }
    }

    scheduleEntries := d.Get("schedule_entries").([]interface{})

    parameters := redis.PatchSchedule{
        ScheduleEntries: &redis.ScheduleEntries{
            ScheduleEntries: expandArmPatchScheduleScheduleEntry(scheduleEntries),
        },
    }


    if _, err := client.CreateOrUpdate(ctx, resourceGroup, name, default, parameters); err != nil {
        return fmt.Errorf("Error creating Patch Schedule %q (Default %q / Resource Group %q): %+v", name, default, resourceGroup, err)
    }


    resp, err := client.Get(ctx, resourceGroup, name, default)
    if err != nil {
        return fmt.Errorf("Error retrieving Patch Schedule %q (Default %q / Resource Group %q): %+v", name, default, resourceGroup, err)
    }
    if resp.ID == nil {
        return fmt.Errorf("Cannot read Patch Schedule %q (Default %q / Resource Group %q) ID", name, default, resourceGroup)
    }
    d.SetId(*resp.ID)

    return resourceArmPatchScheduleRead(d, meta)
}

func resourceArmPatchScheduleRead(d *schema.ResourceData, meta interface{}) error {
    client := meta.(*ArmClient).patchSchedulesClient
    ctx := meta.(*ArmClient).StopContext

    id, err := azure.ParseAzureResourceID(d.Id())
    if err != nil {
        return err
    }
    resourceGroup := id.ResourceGroup
    name := id.Path["Redis"]
    default := id.Path["patchSchedules"]

    resp, err := client.Get(ctx, resourceGroup, name, default)
    if err != nil {
        if utils.ResponseWasNotFound(resp.Response) {
            log.Printf("[INFO] Patch Schedule %q does not exist - removing from state", d.Id())
            d.SetId("")
            return nil
        }
        return fmt.Errorf("Error reading Patch Schedule %q (Default %q / Resource Group %q): %+v", name, default, resourceGroup, err)
    }


    d.Set("name", name)
    d.Set("resource_group", resourceGroup)
    d.Set("default", default)
    if scheduleEntries := resp.ScheduleEntries; scheduleEntries != nil {
        if err := d.Set("schedule_entries", flattenArmPatchScheduleScheduleEntry(scheduleEntries.ScheduleEntries)); err != nil {
            return fmt.Errorf("Error setting `schedule_entries`: %+v", err)
        }
    }
    d.Set("type", resp.Type)

    return nil
}


func resourceArmPatchScheduleDelete(d *schema.ResourceData, meta interface{}) error {
    client := meta.(*ArmClient).patchSchedulesClient
    ctx := meta.(*ArmClient).StopContext


    id, err := azure.ParseAzureResourceID(d.Id())
    if err != nil {
        return err
    }
    resourceGroup := id.ResourceGroup
    name := id.Path["Redis"]
    default := id.Path["patchSchedules"]

    if _, err := client.Delete(ctx, resourceGroup, name, default); err != nil {
        return fmt.Errorf("Error deleting Patch Schedule %q (Default %q / Resource Group %q): %+v", name, default, resourceGroup, err)
    }

    return nil
}

func expandArmPatchScheduleScheduleEntry(input []interface{}) *[]redis.ScheduleEntry {
    results := make([]redis.ScheduleEntry, 0)
    for _, item := range input {
        v := item.(map[string]interface{})
        dayOfWeek := v["day_of_week"].(string)
        startHourUtc := v["start_hour_utc"].(int)
        maintenanceWindow := v["maintenance_window"].(string)

        result := redis.ScheduleEntry{
            DayOfWeek: redis.DayOfWeek(dayOfWeek),
            MaintenanceWindow: utils.String(maintenanceWindow),
            StartHourUtc: utils.Int32(int32(startHourUtc)),
        }

        results = append(results, result)
    }
    return &results
}


func flattenArmPatchScheduleScheduleEntry(input *[]redis.ScheduleEntry) []interface{} {
    results := make([]interface{}, 0)
    if input == nil {
        return results
    }

    for _, item := range *input {
        v := make(map[string]interface{})

        v["day_of_week"] = string(item.DayOfWeek)
        if maintenanceWindow := item.MaintenanceWindow; maintenanceWindow != nil {
            v["maintenance_window"] = *maintenanceWindow
        }
        if startHourUtc := item.StartHourUtc; startHourUtc != nil {
            v["start_hour_utc"] = int(*startHourUtc)
        }

        results = append(results, v)
    }

    return results
}
